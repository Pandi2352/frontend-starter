import type { RouteObject } from 'react-router-dom';

/**
 * Template feature — copy this folder to start a new feature module.
 * Register the routes in src/app/routes.ts.
 */
export const featureRoutes: RouteObject[] = [
  // {
  //   path: '/feature',
  //   lazy: async () => ({ Component: (await import('./pages/FeaturePage')).FeaturePage }),
  // },
];
