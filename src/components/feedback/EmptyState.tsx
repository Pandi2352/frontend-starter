/**
 * Structure-compat re-export — the implementation lives in components/ui/empty-state.
 * Not re-exported from feedback/index.ts to avoid a duplicate barrel export.
 */
export { EmptyState } from '@/components/ui/empty-state';
