/**
 * Global state lives in React Context + useReducer (see src/providers).
 * Add store slices here (auth/, app/, ui/) only when the app outgrows
 * provider-level state — introduce Zustand/Redux only if truly required.
 */
export {};
